# Dead Code — Genuine Findings

Generated by `make dead-code` (vulture, ≥80% confidence). False positives removed
(see notes below).

---

## Dead-code false positives

| Symbol | File | Reason |
|---|---|---|
| `_theta_from_m_align` | `src/hydra_suite/core/canonicalization/resample.py` | Retained as the full-frame reduction reference for `_theta_for_subregion`'s equivalence test (`tests/test_canonical_warp_aabb.py::test_theta_subregion_reduces_to_full_frame`); not called from production code paths. |
| `predict_obb_for_frame` | `src/hydra_suite/detectkit/gui/prediction_preview.py` | Still has no production callers (pre-existing, not introduced by the AL batching work — its sibling `predict_obb_for_frame_export` was the one the AL detector closure used, and that one was deleted with the closure in the AL 3-phase restructure). As of Task 10 of the AL-pipeline-optimization plan, it IS a real, load-bearing test dependency: `tests/test_al_detectkit_equivalence.py` uses it as the OLD-path baseline for a numeric-equivalence check against the new `InferenceRunner`-based AL detection path (it is provably behavior-identical to the retired `predict_obb_for_frame_export` for every field that test compares — see that test's module docstring). Do not delete without first updating/removing that test. `predict_obb_for_frame_sequential` and `load_torch_model` in the same module ARE still live (preview path). |
