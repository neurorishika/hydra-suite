# Shared CUDA packages for hydra-suite
# Do not install directly — use requirements-cuda12.txt or requirements-cuda13.txt.
#
# Base dependencies are in pyproject.toml (installed via -e . below).
# This file adds torch and CUDA-specific packages only.

# PyTorch with CUDA support (index URL set in requirements-cuda12/13.txt)
torch
torchvision
torchaudio

# TensorRT runtime wheels are selected by requirements-cuda12/13.txt.
# Do not install the generic `tensorrt` metapackage here because it currently
# resolves to the CUDA 13 wheel family and can contaminate CUDA 12 envs.

# ONNX runtime with GPU support
onnx
onnxslim
onnxscript
# IMPORTANT: onnxruntime-gpu currently links against CUDA 12 user-space libraries.
# Do not co-install CPU onnxruntime.
onnxruntime-gpu==1.24.1

PyNvVideoCodec
av>=12.0

# Install this package in editable mode (pulls all pyproject.toml dependencies)
-e .
